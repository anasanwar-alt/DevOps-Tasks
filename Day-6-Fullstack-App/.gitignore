# Dependencies — restored by `npm ci` from the committed package-lock.json.
node_modules/

# Build output — regenerated by `npm run build` / `npm test`.
dist/
dist-test/

# Real secrets. .env.example is committed in its place.
.env

# Editor / OS noise
.DS_Store
*.log
