# Template for the git-ignored .env that Compose reads automatically.
#
#   cp .env.example .env     # then edit the values
#
# The real .env is git-ignored — never commit credentials. In a real project
# these would come from a secrets manager rather than a file on disk.
POSTGRES_USER=appuser
POSTGRES_PASSWORD=change-me
POSTGRES_DB=appdb
