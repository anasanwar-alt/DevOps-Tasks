# Compose reads this file automatically and substitutes ${...} in docker-compose.yml.
# Lab credentials only — a real project would keep this out of version control
# (.gitignore) and inject secrets from a secrets manager.
POSTGRES_USER=appuser
POSTGRES_PASSWORD=appsecret
POSTGRES_DB=appdb
